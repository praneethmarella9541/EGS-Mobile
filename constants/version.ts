/**
 * App version shown in the sidebar menu. Bump this manually in git when you
 * ship a new version — deliberately NOT read from app.json/EAS, since EAS
 * autoIncrement only bumps the native build number, not a version you control.
 */
export const APP_VERSION = '1.0.0';
